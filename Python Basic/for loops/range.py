# range defaults 0 as a starting value, however,
# yu can specify a staring valueas well

for x in range(2, 10):
    print (x, '\n')
    
#  range function increments a value by 1 by default, however,
# you can specify the increment value as well
for x in range(2, 50, 2):
    print (x, '\n')